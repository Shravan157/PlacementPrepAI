# ── Supabase PostgreSQL (shared team database — direct connection, not pgbouncer pooler) ──
# Replace <password> with your Supabase project password before running anything.
DATABASE_URL=postgresql://postgres:<password>@db.oxiiikhtfvdglaxnzdut.supabase.co:5432/postgres

# ── JWT ───────────────────────────────────────────────────────────────────────
# Generate a strong secret, e.g.: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=b5b8d33a31314ee9fdb95e96c1935e0fd6810cb499dad65d5ca99237149d7589
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ── Test database (pytest only — SQLite in-memory, no separate server needed) ──
# If you want to run tests against the Docker test_db service instead, change to:
# TEST_DATABASE_URL=postgresql://postgres:test@localhost:5433/test_db
TEST_DATABASE_URL=sqlite:///:memory:
